# -*- mode: snippet -*-
# name: src cli curl
# key: srclic
# --
#+begin_src sh :results output :wrap src json
nu-br-${1:prod} ser curl ${2:get} ${3:\$shard} `(completing-read "Service: " (directory-files (getenv "NU_HOME") nil "^[^.]"))` ${4:/api/${5:customers/${6:\$customer_id}}}$0 | jq
#+end_src
